name: multistep

on:
  push:
    branches: [ main ]

jobs:
  multistep:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: "echo I'm step1"
      - name: step2
        run: "echo I'm step2"
